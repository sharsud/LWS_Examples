# Execution Roadmap

## Phase 1: Legacy Code Migration (Current)
- [x] Extract legacy HTML structure (`Registration.php`).
- [ ] Initialize React + Vite + Tailwind CSS project repository.
- [ ] Migrate `Registration` form into a modern React component (`Registration.jsx`).
- [ ] Convert modal popup and dynamic state handling into React `useState`.

## Phase 2: Automation Sandbox Expansion
- [ ] Build `Login.jsx` with dynamic authentication state.
- [ ] Add explicit `data-testid` attributes to all form controls and triggers.
- [ ] Create dynamic element page (shadow DOM, dynamic delayed loading spinners, drag-and-drop).

## Phase 3: Ecosystem Sync
- [ ] Align navigation headers and footers with main `learnwithpsudo.com` design tokens.
- [ ] Deploy updated sandbox to `examples.learnwithpsudo.com`.